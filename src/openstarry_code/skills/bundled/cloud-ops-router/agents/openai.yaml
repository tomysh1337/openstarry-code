interface:
  display_name: "Cloud and Operations Router"
  short_description: "Route cloud and operations work to exact local skills"
  default_prompt: "Use $cloud-ops-router to select the right cloud or operations workflow."
policy:
  allow_implicit_invocation: true
