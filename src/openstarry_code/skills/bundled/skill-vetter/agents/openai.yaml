interface:
  display_name: "Skill Vetter"
  short_description: "Audit a skill before installation"
  default_prompt: "Use $skill-vetter to audit this skill's metadata, scripts, dependencies, and permissions."
policy:
  allow_implicit_invocation: true
