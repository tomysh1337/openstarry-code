interface:
  display_name: "Content Strategy"
  short_description: "Plan content around goals and audience"
  default_prompt: "Use $content-strategy to build this content plan."
policy:
  allow_implicit_invocation: true
