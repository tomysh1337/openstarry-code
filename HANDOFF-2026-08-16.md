# OpenStarry Code 交接报告

日期：2026-08-16
版本：0.5.7
范围：双自定义 API 配置、模型发现故障回退、本地构建与发布

## 1. 完成结果

- 默认系统身份、网关标题和 CLI 参考已统一为 `OpenStarry Code`；高级设置新增可编辑的调查提示词模板和 Codex-X 状态/共享目录入口。
- 当前活动 Zen API 部署为 `custom_2`，引用 `ZEN_API_KEY_2`；自定义
  Chat Completions 槽位 `custom`、`custom_2`、`custom_3`、`custom_4`
  可分别保存多个 API，不会因首次选择后从目录消失。
- 当前 Base URL 为 `https://www.zen-api.top/v1`，配置文件中没有保存
  Key 明文；其它 API 应通过独立 profile 或环境变量保存。
- 模型 ID 始终来自所选 API 的实时 `/models` 目录，不再写死某个固定模型。
- 当前主服务商保持 `custom_responses`，未被 Zen profile 覆盖。
- 模型实时获取失败后，界面会保留当前 profile 已配置的模型，不再把它
  显示成缺失、空列表或无法识别。
- 实时获取恢复后，`live` 列表会保留当前配置 ID，并合并真实目录。

## 2. 配置位置

桌面配置：

```text
%APPDATA%\@openstarry-code\desktop-electron\openstarry-code\config.toml
```

配置结构：

```toml
[llm]
provider = "custom_2"
display_name = "Zen API 2"
model = "gpt-5.6-sol"
api_key_env = "ZEN_API_KEY_2"
base_url = "https://www.zen-api.top/v1"
```

凭证位于 Windows 用户环境变量。仓库、交接报告、构建产物和
`config.toml` 都没有 Key 明文。

## 3. 源码改动

核心逻辑位于：

- `openstarry-code-webui/src/composables/setup/useSetupProviderForm.ts`
- `src/openstarry_code/provider/openai.py`
- `openstarry-code-webui/src/components/setup/SetupModelCombobox.vue`
- `openstarry-code-webui/src/components/setup/SetupProviderCredentialCard.vue`

行为变化：

1. `ConnectionState.modelSource` 新增 `configured`。
2. 模型发现失败时，从当前 profile 的 `model` 生成单行回退目录。
3. 回退行使用 `capabilitySource: configured`，不伪造上下文、价格或能力。
4. `discoverFailureKind` 与普通连接验证状态分开保存。
5. `transport_transient` 在界面显示为端点网络不可达，同时说明已保留
   保存的模型。
6. 用户编辑模型 ID 时，`configured` 回退行同步更新。
7. 实时发现成功时清除失败状态，并把真实模型目录与当前配置 ID 合并。
8. OpenAI 兼容目录支持 `data`、`models`、根数组等响应外壳，并在版本化
   `/v1/models` 返回 404 时回退到配置根下的 `/models`。
9. WebSocket 消息循环将 Starlette 在并发关闭竞态下产生的固定状态错误按
   正常断开处理，避免把客户端主动关闭误记为 `ws.error`；其他运行时异常
   仍照常记录。

新增回归覆盖：

- 传输失败时保留保存模型。
- 两个 profile 只使用自己的模型回退。
- 实时发现恢复后保留配置 ID 并合并实时目录。
- `configured` 来源可以打开模型下拉，但不标记为 Live。
- 错误提示不直接展示 `ConnectError` 原始文本。
- WebSocket 并发关闭回归通过，固定状态错误不会产生 `ws.error`。
- 高级设置中的调查提示词模板、Codex-X 状态和共享目录入口已通过浏览器运行时检查。

## 4. 外部服务现状

2026-08-16 的真实 RPC 结果（强制刷新）：

```text
custom_2 -> ok=true, source=live, models=gpt-5.5,gpt-5.6-sol,gpt-5.6-terra
candidate custom_3 -> ok=true, source=live, 34 个模型
```

模型目录由中转站按 Key 和时间动态返回；因此某次目录没有某个模型时，应用
不会伪造该 ID，目录恢复后会自动显示。

## 5. 验证记录

- 聚焦 WebUI 测试：134/134 通过。
- 供应商面板与表单回归：147/147 通过。
- 模型发现与 provider 表单回归：Python 93 项、WebUI 60 项通过。
- 全量 WebUI 测试：3,759/3,759 通过，331 个测试文件全部通过。
- TypeScript 与 WebUI 架构检查通过。
- 六种语言的 i18n key parity 通过。
- Vite 生产构建与 WebUI 产物校验通过。
- PyInstaller 网关重新打包完成。
- Windows 打包网关 smoke test 通过。
- E 盘实例替换后 `/healthz`、`/readyz`、`/control/` 均正常，模型发现
  RPC 返回 `ok=true, source=live`，前端使用实时模型目录。
- 安装目录中新旧网关哈希不同，新网关与构建输出哈希一致。
- 配置文件解析通过，Key 明文检查结果为 false。

## 6. 本地部署与回滚

新运行时已同步到：

```text
E:\starry\OpenStarry Code\resources\runtime\gateway\openstarry-code-gateway
```

本次旧运行时回滚副本：

```text
E:\starry\OpenStarry Code\resources\runtime\gateway\openstarry-code-gateway.backup-20260816-164427
```

新网关 SHA-256：

```text
19B40C3E0FAB015DE13BB5CF78F1C462A3E0F17488A95C90D5870CFA7ABDCAEB
```

新桌面 `app.asar` SHA-256：

```text
F30424C2DEBCB2D9CE664FCA7F9227675C0B4408AF096A919F82E4474A89506D
```

本次回滚副本：

```text
E:\starry\OpenStarry Code\resources\app.asar.rollback-20260816-164427
E:\starry\OpenStarry Code\resources\runtime\gateway\openstarry-code-gateway.backup-20260816-164427
```

## 7. 发布信息

- 目标标签：`v0.5.8`
- Release 标题：`OpenStarry Code 0.5.8`
- 发行说明：`docs/releases/0.5.8.md`
- 预期产物：NSIS EXE、WiX MSI、Python wheel、sdist、更新元数据和
  `SHA256SUMS`。
- Windows 安装包当前未签名，下载后以 `SHA256SUMS` 为准。

## 8. 后续检查

Zen 域名恢复后，分别刷新 `Zen API 1` 和 `Zen API 2` 的模型目录：

1. 确认 RPC 返回 `ok=true`、`source=live`。
2. 确认模型下拉由 `configured` 回退行扩展为实时目录，并保留配置 ID。
3. 分别发起一个最小模型请求，确认两枚 Key 的账户与模型权限。
4. 如果服务端实际模型 ID 不同，只修改对应 profile 的 `model`，不要把
   两个环境变量重新合并为 Key 池。
