# OpenStarry Code 交接报告

日期：2026-08-16
版本：0.5.7
范围：双自定义 API 配置、模型发现故障回退、本地构建与发布

## 1. 完成结果

- 本机已把两枚 Zen API 凭证拆成两个独立 profile：`custom` 和
  `custom_2`。
- 两个 profile 分别引用 `ZEN_API_KEY_1`、`ZEN_API_KEY_2`，配置文件中
  没有保存 Key 明文。
- 两个 profile 的 Base URL 均为 `https://www.zen-api.com/v1`，保存模型
  均为 `deepseek-v4-flash`。
- 当前主服务商保持 `custom_responses`，未被 Zen profile 覆盖。
- 模型实时获取失败后，界面会保留当前 profile 已配置的模型，不再把它
  显示成缺失、空列表或无法识别。
- 实时获取恢复后，`live` 列表会正常覆盖 `configured` 回退列表。

## 2. 配置位置

桌面配置：

```text
%APPDATA%\@openstarry-code\desktop-electron\openstarry-code\config.toml
```

配置结构：

```toml
[llm_profiles.custom]
display_name = "Zen API 1"
model = "deepseek-v4-flash"
api_key_env = "ZEN_API_KEY_1"
base_url = "https://www.zen-api.com/v1"

[llm_profiles.custom_2]
display_name = "Zen API 2"
model = "deepseek-v4-flash"
api_key_env = "ZEN_API_KEY_2"
base_url = "https://www.zen-api.com/v1"
```

凭证位于 Windows 用户环境变量。仓库、交接报告、构建产物和
`config.toml` 都没有 Key 明文。

## 3. 源码改动

核心逻辑位于：

- `openstarry-code-webui/src/composables/setup/useSetupProviderForm.ts`
- `openstarry-code-webui/src/components/setup/SetupModelCombobox.vue`
- `openstarry-code-webui/src/components/setup/SetupProviderCredentialCard.vue`

行为变化：

1. `ConnectionState.modelSource` 新增 `configured`。
2. 模型发现失败时，从当前 profile 的 `model` 生成单行回退目录。
3. 回退行使用 `capabilitySource: configured`，不伪造上下文、价格或能力。
4. `discoverFailureKind` 与普通连接验证状态分开保存。
5. `transport_transient` 在界面显示为端点网络不可达，同时说明已保留
   保存的模型。
6. 用户编辑模型 ID 时，`configured` 回退行同步更新。
7. 实时发现成功时清除失败状态，并以真实模型目录替换回退行。

新增回归覆盖：

- 传输失败时保留保存模型。
- 两个 profile 只使用自己的模型回退。
- 实时发现恢复后覆盖回退。
- `configured` 来源可以打开模型下拉，但不标记为 Live。
- 错误提示不直接展示 `ConnectError` 原始文本。

## 4. 外部服务现状

2026-08-16 的真实 RPC 结果：

```text
custom   -> transport_transient, source=none, models=0
custom_2 -> transport_transient, source=none, models=0
```

本地凭证解析已完成，否则结果会是 `auth_invalid`。当前失败发生在远端连接
阶段。此前 DNS 验证显示 `zen-api.com` 权威解析返回 `SERVFAIL`，因此应用
侧保留真实网络故障，不把它伪装成模型发现成功。

## 5. 验证记录

- 聚焦 WebUI 测试：134/134 通过。
- 全量 WebUI 测试：3,759/3,759 通过，331 个测试文件全部通过。
- TypeScript 与 WebUI 架构检查通过。
- 六种语言的 i18n key parity 通过。
- Vite 生产构建与 WebUI 产物校验通过。
- PyInstaller 网关重新打包完成。
- Windows 打包网关 smoke test 通过。
- 安装目录中新旧网关哈希不同，新网关与构建输出哈希一致。
- 配置文件解析通过，Key 明文检查结果为 false。

## 6. 本地部署与回滚

新运行时已同步到：

```text
E:\openstarry\OpenStarry Code\resources\runtime\gateway\openstarry-code-gateway
```

旧运行时保留在：

```text
E:\openstarry\OpenStarry Code\resources\runtime\gateway\openstarry-code-gateway.rollback-20260816-003347
```

新网关 SHA-256：

```text
8154FA1CB1E4833FE582D48B4C43B85102CCCD2092B471F4D10FC50E4E01210A
```

## 7. 发布信息

- 目标标签：`v0.5.7`
- Release 标题：`OpenStarry Code 0.5.7`
- 发行说明：`docs/releases/0.5.7.md`
- 预期产物：NSIS EXE、WiX MSI、Python wheel、sdist、更新元数据和
  `SHA256SUMS`。
- Windows 安装包当前未签名，下载后以 `SHA256SUMS` 为准。

## 8. 后续检查

Zen 域名恢复后，分别刷新 `Zen API 1` 和 `Zen API 2` 的模型目录：

1. 确认 RPC 返回 `ok=true`、`source=live`。
2. 确认模型下拉由单个 `configured` 回退行切换为实时目录。
3. 分别发起一个最小模型请求，确认两枚 Key 的账户与模型权限。
4. 如果服务端实际模型 ID 不同，只修改对应 profile 的 `model`，不要把
   两个环境变量重新合并为 Key 池。
